公共模块：数据采集项目中原始信息表的更新
更新步骤(总体):
       a、确定更新周期：待定
       b、基本流程：
            1、建临时表用于对比（表名带有日期作为标识，表过期时删除）
            2、调度爬虫采集信息到临时表
            3、查询临时表与原始信息表对比:相同:无动作，不同:执行insert
            4、检查表是否过期:是:drop 否:无动作
